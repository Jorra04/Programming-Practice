#include <stdlib.h>
#include <stdio.h>

void main(){
    int myArr[3][3]; 
    int count = 0;
    for(int i = 0; i < 3; i ++ ){
        for(int j = 0; j < 3; j ++ ){
            myArr[i][j] = count;
            count ++;
            // printf("%d ", myArr[i][j]);
        }
        // printf("\n");
    }

    for(i =0; i < 3; i ++ ){
        for(j = 0; j < 3; j ++ ){
            
        }
    }

}